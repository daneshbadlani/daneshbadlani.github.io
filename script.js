// script.js

// Example: Add event listener for the blog button if needed
document.addEventListener('DOMContentLoaded', () => {
    const blogButton = document.querySelector('.btn');
    blogButton.addEventListener('click', () => {
        alert('Redirecting to blog...');
    });
});
